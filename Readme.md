
## Usage

#### Install cli commands
`make install-cli`

#### Run server
`make run-server`

#### Start cli client
`cli --help`

#### Start tests
`cli test`

#### Get rank (percentile)
`cli percentile <name>`